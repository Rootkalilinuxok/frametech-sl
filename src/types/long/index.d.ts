// src/types/long/index.d.ts
declare module "long" {
  // Qui puoi aggiungere dichiarazioni più precise, se ti servono,
  // ma anche solo questo basta per far sparire l’errore di TS.
  const Long: any;
  export default Long;
}
